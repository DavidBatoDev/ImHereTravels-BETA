import type { Step1PersonalReservationSectionProps } from "../../components/Step1PersonalReservationSection";

export type UseStep1SectionPropsOptions = Step1PersonalReservationSectionProps;

export const useStep1SectionProps = (
  options: UseStep1SectionPropsOptions,
): Step1PersonalReservationSectionProps => {
  return options;
};

